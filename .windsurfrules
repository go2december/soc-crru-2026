# SOC-CRRU Windsurf & Antigravity Rules

## Project Context
- This repository is a full-stack web application for the Faculty of Social Sciences, Chiang Rai Rajabhat University.
- Frontend lives in `frontend/` using Next.js 16.1.1 App Router, React 19.2.3, TypeScript, Tailwind CSS v4, and DaisyUI v5.5.14.
- Backend lives in `backend/` using NestJS on Node.js v25, TypeScript, Drizzle ORM, and PostgreSQL 18.
- Local development is primarily based on `docker-compose.yml` with 4 services.

## Source of Truth
Before making medium or large changes, review these files first in this order:
1. `docs/WORKFLOW-project-status.md`
2. `docs/PLAN-soc-crru-baseline.md`
3. `docs/PLAN-chiang-rai-studies.md`
4. `docs/WORKFLOW-chiang-rai-admin.md`
5. `docs/WORKFLOW-faculty-admin.md`
6. `.windsurf/workflows/*.md` (or `.agent/workflows/*.md`)

If there is a conflict between current implementation notes and older documentation, update the outdated documentation as part of the task when appropriate.

## Working Rules
- Prefer minimal, targeted changes over broad refactors.
- Preserve the current project structure and naming patterns.
- Do not introduce new architectural patterns unless the task requires them.
- Keep frontend and backend changes aligned when modifying shared behaviors.
- When changing a central API contract, inspect likely consumers in `frontend/` and update them together.
- Do not hardcode secrets or environment-specific credentials.

## Frontend Rules
- Use App Router conventions in `frontend/app/`.
- Reuse existing shared components from `frontend/components/` before creating new ones.
- Reuse utilities from `frontend/lib/` when possible.
- Keep TypeScript types explicit for API-driven data.
- Prefer server-friendly data fetching patterns and avoid unnecessary client-only logic.
- **UI Framework Separation**: Use DaisyUI for main public pages and shadcn/ui for Admin Dashboard
  - **DaisyUI**: Public pages (`/`, `/about/*`, `/programs/*`, `/academics/*`, `/chiang-rai-studies/*` public routes)
  - **shadcn/ui**: Admin dashboards (`/admin/*`, `/chiang-rai-studies/admin/*`)
  - Import shadcn/ui components only in admin routes and layouts
  - Use DaisyUI classes and themes for public-facing content
- Do not introduce a new UI library unless explicitly required.

## Backend Rules
- Keep NestJS module, controller, and service responsibilities clear.
- Put backend source changes under `backend/src/`.
- Respect existing validation and DTO patterns.
- Assume the backend uses a global API prefix: `/api`.
- Be careful with static upload paths and any logic related to `/uploads`.
- Keep database access patterns consistent with the existing Drizzle-based implementation.

## UI Framework Rules
- **DaisyUI Public Pages**: Use DaisyUI components and classes for all public-facing routes
  - Faculty public pages: `/`, `/about/*`, `/programs/*`, `/academics/*`, `/admissions`, `/research/*`, `/eservice/*`
  - Chiang Rai Studies public pages: `/chiang-rai-studies/*` (except admin sub-routes)
  - Use DaisyUI themes (`socTheme`) and design tokens for consistent branding
- **shadcn/ui Admin Dashboards**: Use shadcn/ui components exclusively in admin interfaces
  - Faculty admin: `/admin/*` routes
  - Chiang Rai Studies admin: `/chiang-rai-studies/admin/*` routes
  - Import from `frontend/components/ui/` only in admin layouts and pages
- **Styling Separation**:
  - Public pages: Use `@layer base` and DaisyUI utility classes from `globals.css`
  - Admin pages: Use shadcn/ui CSS variables and component patterns
  - Never mix DaisyUI classes with shadcn/ui components in the same component
- **Component Location**:
  - Public components: `frontend/components/` (avoid `ui/` subdirectory)
  - Admin components: `frontend/components/ui/` for shadcn/ui components
- **Theme Consistency**:
  - Public pages maintain Scholar theme and Chiang Rai brand colors
  - Admin pages use professional slate theme with brand accent colors

## Local Skill Usage Rules
- Use `.windsurf/skills/ui-ux-pro-max` (or `.agent/skills/ui-ux-pro-max`) when the task is primarily about UI polish, layout refinement, visual hierarchy, responsive refinement, or UX improvement.
- When using `ui-ux-pro-max`, always apply SOC-CRRU route constraints first:
  - public routes must stay on DaisyUI
  - admin routes must stay on shadcn/ui
- Prefer project-specific local skills together with `ui-ux-pro-max` rather than replacing them:
  - combine with `soc-crru-public-frontend` for public page work
  - combine with `soc-crru-admin-dashboard` for admin screens
  - combine with `soc-crru-seo-metadata` if layout/content changes affect metadata or public SEO
- Do not use `ui-ux-pro-max` as justification to introduce a new UI library, change route structure, or break current branding constraints.

## Skill Map
Use this map to choose the primary skill first, then add companion skills when the task crosses surfaces.

### Public frontend tasks
- Primary: `soc-crru-public-frontend`
- Add `ui-ux-pro-max` for UI polish, layout refinement, or responsive improvements
- Add `tailwind-patterns` for Tailwind v4 token, layout, or utility architecture work
- Add `nextjs-react-expert` for App Router, SSR, client boundary, or performance-sensitive route work
- Add `soc-crru-seo-metadata` when public route metadata, sitemap, robots, canonical, or JSON-LD may be affected

### Admin dashboard tasks
- Primary: `soc-crru-admin-dashboard`
- Add `ui-ux-pro-max` for admin UX polish, form layout, visual hierarchy, or dashboard refinement
- Add `nextjs-react-expert` for interactive React behavior, route logic, or performance issues
- Add `lint-and-validate` before completion
- Add `systematic-debugging` when the task is primarily a bug investigation rather than straightforward implementation

### Backend API and data tasks
- Primary: `soc-crru-backend-nest-drizzle`
- Add `api-patterns` when changing endpoints, response shapes, pagination, or auth-sensitive API behavior
- Add `lint-and-validate` before completion
- Add `systematic-debugging` for API mismatches, DTO drift, auth bugs, or multi-surface regressions

### Chiang Rai Studies CRUD tasks
- Primary: `soc-crru-chiang-rai-crud`
- Add `soc-crru-backend-nest-drizzle` for backend service/controller/schema changes
- Add `api-patterns` for route or contract changes
- Add `soc-crru-media-upload-cleanup` when uploads, thumbnails, galleries, or cleanup logic are involved
- Add `soc-crru-seo-metadata` when public list/detail pages, tags, metadata, sitemap, or JSON-LD are affected
- Add `lint-and-validate` before completion

### Media upload and cleanup tasks
- Primary: `soc-crru-media-upload-cleanup`
- Add `soc-crru-admin-dashboard` or `soc-crru-chiang-rai-crud` based on the route surface
- Add `soc-crru-public-frontend` if public rendering behavior is also affected
- Add `systematic-debugging` when media bugs are inconsistent across admin, SSR, and client rendering

### SEO and metadata tasks
- Primary: `soc-crru-seo-metadata`
- Add `soc-crru-public-frontend` for public route implementation work
- Add `ui-ux-pro-max` if layout/content restructuring changes visual hierarchy or snippet quality
- Add `nextjs-react-expert` for route-level metadata, App Router behavior, or performance-sensitive SEO work

### Debugging tasks
- Primary: `systematic-debugging`
- Add the surface-specific skill next:
  - `soc-crru-public-frontend` for public page bugs
  - `soc-crru-admin-dashboard` for admin bugs
  - `soc-crru-backend-nest-drizzle` for backend/API bugs
  - `soc-crru-chiang-rai-crud` for Chiang Rai-specific bugs
  - `soc-crru-media-upload-cleanup` for upload or media cleanup bugs
- Add `lint-and-validate` after the fix is implemented

### Validation and completion tasks
- Primary: `lint-and-validate`
- Always pair it with the implementation surface skill that matches the changed area
- Do not mark work complete before relevant lint, targeted verification, and affected flow checks are done

### Documentation and workflow sync tasks
- Primary: `soc-crru-docs-governance`
- Add the implementation surface skill that changed project behavior or status
- Add `soc-crru-seo-metadata` if public SEO behavior changed
- Update `.windsurf/workflows/` (and `.agent/workflows/`) only when executable steps changed

## API and Integration Rules
- Frontend requests to backend endpoints should account for the `/api` prefix.
- Be careful with local vs Docker networking assumptions.
- For SSR or server-side fetches, verify whether the code should use internal Docker URLs or localhost fallbacks.
- If you change endpoint paths, update all affected callers and admin pages.

## Admin CRUD Rules
- Treat admin work as multi-surface changes: verify list, create, edit, delete, dashboard stats, and affected public pages together.
- For admin changes, inspect both the frontend admin route and the matching backend controller/service before editing.
- Preserve current route group structure and naming patterns in `frontend/app/admin/` and `frontend/app/chiang-rai-studies/admin/`.
- Do not change admin route paths, callback paths, or dashboard structure unless the task explicitly requires it.
- Keep admin forms aligned with backend DTOs, validation expectations, enum values, and required fields.
- When a CRUD entity exposes slug-based public pages, verify both admin by-id flows and public slug/detail flows.
- When deleting records with uploaded media, verify whether image cleanup must happen before or alongside record deletion.
- Keep confirmation, loading, and error states intact on destructive admin actions.

## Faculty Admin Rules
- Faculty admin lives under `/admin` and currently covers `dashboard`, `staff`, `departments`, `positions`, `users`, and `news`.
- Preserve the auth flow `/admin/login` -> `/api/auth/google` or `/api/auth/dev/login` -> `/admin/callback` -> `/admin/(dashboard)/dashboard`.
- For Faculty admin API work, verify corresponding endpoints under `/api/staff`, `/api/departments`, `/api/news`, and related auth endpoints.
- If you change faculty news behavior, also inspect public news listing/detail consumers and keep admin/public behavior aligned.
- If staff or profile image behavior changes, preserve relative-path storage and verify both admin preview and public rendering.

## Chiang Rai Studies Rules
- Chiang Rai public pages live under `/chiang-rai-studies`, and admin pages live under `/chiang-rai-studies/admin`.
- Treat `backend/src/chiang-rai/` as the authoritative backend area for Chiang Rai Studies feature work.
- Preserve consistency between public pages, admin CRUD pages, homepage config, tags, search, and stats when changing Chiang Rai behavior.
- Keep entity names and API namespaces consistent with the existing `chiang-rai` naming convention in both frontend and backend.
- When changing Chiang Rai content models, inspect likely public consumers such as homepage sections, list pages, detail pages, sitemap, metadata, and JSON-LD generation.

## Agent CRUD Checklists
### Shared CRUD Checklist
- Confirm affected frontend admin page, backend endpoint, and public consumer.
- Check list, create, edit, delete, and by-id/by-slug flows that belong to the same entity.
- Verify form fields still match DTOs, enums, required fields, and backend validation.
- Verify success, loading, empty, and error states still behave correctly.
- If media exists, verify preview, persistence, replacement, and cleanup behavior.

### UI Framework Checklist
- **Public Pages**: Use DaisyUI components, classes, and `socTheme` consistently
- **Admin Pages**: Use shadcn/ui components from `frontend/components/ui/` only
- **Component Imports**: Never import shadcn/ui in public routes or DaisyUI in admin routes
- **Styling**: Public pages use DaisyUI utilities, Admin pages use shadcn/ui CSS variables
- **Theme Colors**: Public pages use Scholar/Chiang Rai branding, Admin pages use slate theme

### Faculty: Staff
- Check admin list, create/edit form, delete flow, and public staff rendering.
- Verify department, position, and profile image fields still resolve correctly.

### Faculty: Departments
- Check admin list, create, edit, and delete behavior.
- Verify related staff or public references do not break after changes.

### Faculty: Positions
- Check admin list, create, edit, and delete behavior.
- Verify staff forms and displayed labels still use the updated position data correctly.

### Faculty: Users
- Check admin list and any supported create/edit/delete behavior.
- Verify auth-related fields or role/permission assumptions remain compatible with current flows.

### Faculty: News
- Check admin list, create, edit, and delete behavior.
- Verify public listing/detail consumers, if affected, still render correctly.

### Chiang Rai: Articles
- Check admin list, create, edit, delete, `by-id` fetch, and public slug detail page.
- Verify slug, tags, publish status, metadata, and sitemap/detail consumers if affected.
- Articles CRUD completed 2026-02-13 with full admin and public flows.

### Chiang Rai: Artifacts
- Check admin list, create, edit, and delete behavior.
- Verify category mapping, archive filters, and artifact detail compatibility.

### Chiang Rai: Activities
- Check admin list, create, edit, delete, `by-id` fetch, and public slug detail page.
- Verify type enum, pagination, event date/location fields, and image cleanup behavior.

### Chiang Rai: Learning Sites
- Check admin list, create, edit, delete, `by-id` fetch, and public slug detail page.
- Verify thumbnail, media gallery, content images, tags, and removed-image cleanup behavior.

### Chiang Rai: Staff
- Check admin list, import, delete, and grouped display behavior.
- Verify faculty import compatibility and role/group mapping.

### Chiang Rai: Settings
- Check settings form load/save behavior.
- Verify homepage hero image, title, subtitle, and dependent homepage sections still render correctly.

## Media and Uploaded Image Rules
- Store uploaded file references as relative paths only, especially under `/uploads/chiang-rai/...` and other project upload namespaces.
- Do not store backend-only absolute URLs in the database.
- For admin client rendering, verify URL resolution against `NEXT_PUBLIC_API_URL` conventions.
- For SSR or server-rendered public pages, verify URL resolution against `INTERNAL_API_URL` or the current fallback strategy.
- When replacing or deleting media in rich content, also consider embedded content images, thumbnails, and gallery arrays rather than only the primary image field.
- Be careful not to orphan files on disk when edit/delete behavior is expected to perform cleanup.

## Documentation and Workflow Rules
After any medium or large task:
- update `docs/WORKFLOW-project-status.md` when project status changes
- update the relevant `docs/PLAN-*.md` phase/checklist when feature status changes
- update the relevant `docs/WORKFLOW-*.md` file when operational behavior changes
- update `.windsurf/workflows/*.md` (and `.agent/workflows/*.md`) only when executable steps changed

When adding a new operational process:
- document it in `docs/WORKFLOW-*.md`
- add or update a matching executable workflow in `.windsurf/workflows/` (and `.agent/workflows/`)

## Status Taxonomy
Use only these labels in plan/status documents:
- `✅ COMPLETE`
- `🔄 IN PROGRESS`
- `📋 PENDING`

For checklists, use:
- `[x]` completed
- `[ ]` pending

## Validation Rules
Before finishing implementation when relevant:
- run frontend lint from `frontend/`
- run backend lint or targeted tests from `backend/`
- avoid claiming success without verifying the affected area
- **UI Framework Validation**:
  - Verify public pages use only DaisyUI components and classes
  - Verify admin pages use only shadcn/ui components from `frontend/components/ui/`
  - Check there are no cross-imports between UI frameworks inappropriately
  - Test theme consistency across public (Scholar/Chiang Rai) and admin (slate) pages

## Safety Rules
- Do not delete or rename major directories without explicit instruction.
- Do not overwrite large content sections or seeded data unless the task specifically requires it.
- Be careful around admin CRUD flows, image uploads, and route changes because they often affect multiple pages.

## Preferred Task Flow
1. Read the relevant plan/workflow docs.
2. Inspect the authoritative code path.
3. Make the smallest correct implementation.
4. Verify affected frontend/backend behavior.
5. Sync docs and workflows if the task meaningfully changed project status or process.
