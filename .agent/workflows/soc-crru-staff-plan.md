---
description: แผนงานและโครงสร้างข้อมูลระบบจัดการบุคลากร (Staff Management System)
---

# 👥 SOC-CRRU Staff Management System Plan

เอกสารนี้รวบรวมโครงสร้างข้อมูล (Data Structure), ฟีเจอร์, และแผนการพัฒนาสำหรับระบบจัดการบุคลากรของคณะสังคมศาสตร์ มรภ.เชียงราย

## 1. Data Structure (โครงสร้างข้อมูล)
อ้างอิงจาก Database Schema ปัจจุบัน (`staff_profiles`)

### 1.1 ข้อมูลส่วนตัว (Personal Info)
| Field | Type | Description |
|-------|------|-------------|
| `prefixTh` | String | คำนำหน้าชื่อ (ไทย) เช่น นาย, นางสาว, ดร., ผศ. |
| `firstNameTh` | String | ชื่อจริง (ไทย) **(Required)** |
| `lastNameTh` | String | นามสกุล (ไทย) **(Required)** |
| `prefixEn` | String | คำนำหน้าชื่อ (อังกฤษ) เช่น Mr., Ms., Dr. |
| `firstNameEn` | String | ชื่อจริง (อังกฤษ) |
| `lastNameEn` | String | นามสกุล (อังกฤษ) |
| `imageUrl` | String (URL) | ลิงก์รูปภาพโปรไฟล์ (รองรับ Upload หรือ URL) |
| `bio` | Text | ประวัติย่อหรือคำแนะนำตัว |

### 1.2 สถานะและการทำงาน (Status & Work)
| Field | Type | Description |
|-------|------|-------------|
| `staffType` | Enum | ประเภทบุคลากร: `ACADEMIC` (สายวิชาการ), `SUPPORT` (สายสนับสนุน) |
| **`departmentId`** | Integer (FK) | รหัสหน่วยงานต้นสังกัด (เชื่อมโยงกับตาราง Departments) |
| `academicPosition` | String/Enum | ตำแหน่งวิชาการ (เฉพาะสายวิชาการ): อาจารย์, ผศ., รศ., ศ. |
| `adminPosition` | String | ตำแหน่งบริหาร / หน้าที่ (เลือกจาก Dropdown: `admin_positions`) เช่น คณบดี, หัวหน้าสาขา |
| **`isExecutive`** | Boolean | **สถานะผู้บริหาร**: `true` = เป็นคณะกรรมการบริหาร/ผู้บริหารระดับสูง (School Executive Committee) |

### 1.3 การศึกษาและความเชี่ยวชาญ (Education & Expertise)
| Field | Type | Description |
|-------|------|-------------|
| `education` | JSON Array | รายการวุฒิการศึกษา `{ level: 'BACHELOR'|'MASTER'|'DOCTORAL', detail: 'ชื่อวุฒิและสถาบัน' }` |
| `expertise` | String Array | รายการความเชี่ยวชาญ เช่น `['Sociology', 'Statistics']` |

### 1.4 การติดต่อและระบบ (Contact & System)
| Field | Type | Description |
|-------|------|-------------|
| `contactEmail` | String | อีเมลสำหรับติดต่อ (Public) |
| `userId` | UUID (FK) | เชื่อมโยงกับบัญชีผู้ใช้ระบบ (User Account) เพื่อให้ Login เข้ามาแก้ไขข้อมูลได้ |
| `sortOrder` | Integer | ลำดับการแสดงผล (Default: 0) |

## 2. Features Implementation (ฟีเจอร์ที่พัฒนาแล้ว)

### 2.1 Backend (NestJS + Drizzle ORM)
- [x] **CRUD API**: `/api/staff` (Create, Read, Update, Delete)
- [x] **Department Association**: เชื่อมโยงกับตาราง Departments
- [x] **Admin Positions**: Module จัดการชื่อตำแหน่งบริหาร (`/api/admin-positions`)
- [x] **Executive Filters**: รองรับการกรองตาม `isExecutive` และ `staffType`
- [x] **User Linking**: ระบบป้องกันการผูกบัญชีซ้ำ (One-to-One Staff-User)
- [x] **Image Upload API**: `/api/upload/staff` 
    - รองรับอัปโหลดไฟล์รูปภาพ
    - รองรับอัปโหลดไฟล์รูปภาพ
    - ใช้ Sharp ในการ Resize (Max 768x1024) และแปลงเป็น PNG
    - **Auto Delete**: ลบรูปภาพเดิมอัตโนมัติเมื่อมีการอัปเดตรูปใหม่
    - Serve Static Files จาก `/uploads/staff/`
    - ✅ **Fixed (2026-02-03)**: Dependencies (uuid, sharp), Type imports (Express.Multer.File)

### 2.2 Frontend (Next.js Admin Panel - Modernized with shadcn/ui)
- [x] **Manage Staff Page** (`/admin/staff`):
    - ตารางรายชื่อพร้อมค้นหาและกรอง
    - แสดงป้ายกำกับ (Badge) สำหรับผู้บริหาร
- [x] **Staff Form**:
    - รองรับการกรอกข้อมูล 2 ภาษา (TH/EN)
    - **Separate Executive Toggle**: Checkbox แยกสถานะผู้บริหารออกจากสังกัด
    - Dynamic Dropdown สำหรับเลือกสังกัด
    - **Image Upload**: File Input พร้อม Preview (1:1 Aspect Ratio)
    - **Education List Builder**: เพิ่ม/ลบ รายการวุฒิการศึกษาแบบ Dynamic
    - **Expertise Tag Input**: ระบบ Tag สำหรับความเชี่ยวชาญ
    - **Position Dropdown**: เลือกตำแหน่งบริหารจากรายการที่กำหนดไว้ (Admin Positions)
    - อัปเดต Label: "ตำแหน่งบริหาร / ตำแหน่งหน้าที่" รองรับทั้งสายวิชาการและสนับสนุน
- [x] **Manage Positions** (`/admin/positions`):
    - ระบบเพิ่ม/ลบ/แก้ไข รายชื่อตำแหน่งบริหาร
    - กำหนดลำดับ (Level) สำหรับการเรียงชื่อใน Dropdown
- [x] **Manage Departments** (`/admin/departments`):
    - ระบบจัดการเพิ่ม/ลบ/แก้ไข รายชื่อสังกัด (Departments)
    - กำหนดประเภทหน่วยงาน (สายวิชาการ/สนับสนุน)

### 2.3 Admin Sidebar
- [x] **Grouped Navigation**: จัดกลุ่มเมนูเป็นหมวดหมู่
    - ภาพรวม (หน้าหลัก)
    - ข้อมูลองค์กร (บุคลากร, จัดการสังกัด)
    - เนื้อหาเว็บไซต์ (ข่าวสาร, หลักสูตร)
    - ระบบ (จัดการผู้ใช้, โปรไฟล์)

## 3. Future Roadmap (แผนพัฒนาต่อไป)
- [ ] **Public Staff Page**: อัปเดตหน้าแสดงผลบนเว็บหลัก (`/about/staff`) ให้ดึงข้อมูลจาก API ใหม่
    - [ ] แสดงแยกแท็บ: ผู้บริหาร, หัวหน้าสาขา, อาจารย์ประจำสาขา, สายสนับสนุน
- [ ] **Staff Profile Detail**: หน้า Profile แยกรายบุคคล (`/about/staff/[id]`) แสดงประวัติโดยละเอียด
- [ ] **Bulk Import**: ระบบนำเข้าข้อมูลจาก Excel/CSV
- [ ] **Sort Order UI**: หน้า UI สำหรับลากวางจัดลำดับ (Sort Order)
- [ ] **Image Cropper**: เพิ่ม UI สำหรับ Crop รูปภาพก่อนอัปโหลด

## 4. Technical Notes (หมายเหตุทางเทคนิค)
### Docker Volume Configuration
- ใช้ anonymous volume `/usr/src/app/node_modules` เพื่อให้ native modules (เช่น sharp) ถูก compile ใน Alpine Linux container
- Host's node_modules แยกจาก container's node_modules

### Dependencies Required for Image Upload
```json
{
  "dependencies": {
    "sharp": "^0.33.2",
    "uuid": "^9.0.0",
    "@nestjs/serve-static": "^5.0.1"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "@types/multer": "^1.4.11"
  }
}
```

---
*Last Updated: 2026-02-04 (Admin Position Module, Image Auto-Delete)*