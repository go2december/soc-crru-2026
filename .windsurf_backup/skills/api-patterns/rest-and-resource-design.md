# REST and Resource Design

## Core Rules

- Prefer resource-oriented endpoint naming.
- Keep namespace conventions aligned with current modules, such as `/api/chiang-rai/...` and `/api/staff`.
- Use HTTP methods consistently with the existing backend style.

## Review Focus

- Controller route naming
- By-id vs by-slug route distinction
- Cross-surface consistency between backend modules and frontend fetch calls
